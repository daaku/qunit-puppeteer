name: Build
on: [push]
jobs:
  build:
    name: Test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-node@v3.5.0
      with:
        node-version: '16'
    - uses: actions/checkout@v3
    - run: npm install
    - run: npm test
